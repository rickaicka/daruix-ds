@mixin custom-scrollbar(
  $width: 8px,
  $track-color: var(--drx-scrollbar-track-color, transparent),
  $thumb-color: var(--drx-scrollbar-thumb-color, var(--drx-color-primary)),
  $thumb-hover: var(--drx-scrollbar-thumb-hover-color, var(--drx-color-primary-hover))
) {
  &::-webkit-scrollbar {
    width: $width;
    height: $width;
  }

  &::-webkit-scrollbar-track {
    background: $track-color;
  }

  &::-webkit-scrollbar-thumb {
    background-color: $thumb-color;
    border-radius: var(--drx-scrollbar-radius, var(--drx-radius-pill));
    border: 2px solid transparent;
    background-clip: content-box;
  }

  &::-webkit-scrollbar-thumb:hover {
    background-color: $thumb-hover;
  }

  scrollbar-width: thin;
  scrollbar-color: $thumb-color $track-color;
}
